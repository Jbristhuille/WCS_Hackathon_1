import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'wcs_hackathon_travel',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
